<?xml version="1.0" encoding="utf-8"?>
<!-- Primary transport buttons: play/pause and reset.
     44x44dp circular, 1.5dp border (docs/design/README.md "Control sizing").
     Deliberately larger than @drawable/icon_button_secondary_background so the
     transport row stays the dominant control affordance in the clock window. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/icon_state_active_background">
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
        </shape>
    </item>
    <item>
        <selector>
            <item android:state_enabled="false">
                <shape android:shape="oval">
                    <solid android:color="@android:color/transparent" />
                    <stroke
                        android:width="@dimen/icon_button_border_width"
                        android:color="@color/icon_state_disabled" />
                </shape>
            </item>
            <item android:state_activated="true">
                <shape android:shape="oval">
                    <solid android:color="@color/icon_state_active_background" />
                    <stroke
                        android:width="@dimen/icon_button_border_width"
                        android:color="@color/icon_state_active" />
                </shape>
            </item>
            <item android:state_selected="true">
                <shape android:shape="oval">
                    <solid android:color="@color/icon_state_active_background" />
                    <stroke
                        android:width="@dimen/icon_button_border_width"
                        android:color="@color/icon_state_active" />
                </shape>
            </item>
            <item>
                <shape android:shape="oval">
                    <solid android:color="@android:color/transparent" />
                    <stroke
                        android:width="@dimen/icon_button_border_width"
                        android:color="@color/icon_state_inactive" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
